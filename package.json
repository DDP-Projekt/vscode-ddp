{
	"name": "vscode-ddp",
	"displayName": "DDP",
	"description": "Language Support for DDP",
	"version": "0.0.1",
	"author": {
		"name": "bafto and NotLe0n",
		"url": "https://github.com/DDP-Projekt"
	},
	"publisher": "DDP-Projekt",
	"engines": {
		"vscode": "^1.68.0"
	},
	"categories": [
		"Programming Languages",
		"Snippets"
	],
	"main": "./out/extension.js",
	"contributes": {
		"languages": [
			{
				"id": "ddp",
				"extensions": [
					".ddp"
				],
				"aliases": [
					"ddp",
					"DDP",
					"Die Deutsche Programmiersprache"
				],
				"configuration": "./language-configuration.json",
				"icon": {
					"light": "./icons/icon.png",
					"dark": "./icons/icon.png"
				}
			}
		],
		"grammars": [
			{
				"language": "ddp",
				"scopeName": "source.ddp",
				"path": "./syntaxes/ddp.tmLanguage.json"
			}
		],
		"snippets": [
			{
				"language": "ddp",
				"path": "./snippets/snippets.json"
			}
		],
		"commands": [
			{
				"command": "ddp.languageserver.restart",
				"title": "DDP: Restart Language Server",
				"description": "Restart the running instance of the language server"
			},
			{
				"command": "ddp.run.file",
				"title": "DDP: Run the current file",
				"description": "Runs the currently open file using $kddp starte",
				"icon": "$(run)"
			}
		],
		"configuration": {
			"title": "DDP",
			"properties": {
				"ddp.DDPPATH": {
					"type": "string",
					"default": "",
					"description": "overrides the environment variable DDPPATH locally"
				},
				"ddp.run.gccOptionen": {
					"type": "string",
					"default": "",
					"description": "gcc_optionen that are passed to $kddp starte as --gcc_optionen"
				},
				"ddp.run.externeGccOptionen": {
					"type": "string",
					"default": "",
					"description": "externe_gcc_optionen that are passed to $kddp starte as --externe_gcc_optionen"
				},
				"ddp.run.wortreich": {
					"type": "boolean",
					"default": false,
					"description": "adds the --wortreich flag to $kddp starte"
				}
			}
		},
		"menus": {
			"editor/title/run": [
				{
					"command": "ddp.run.file",
					"when": "resourceLangId == ddp",
					"group": "navigation"
				}
			]
		}
	},
	"activationEvents": [
		"onLanguage:ddp"
	],
	"dependencies": {
		"vscode-languageclient": "^7.0.0"
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"lint": "eslint src --ext ts",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/vscode": "^1.68.0",
		"@types/glob": "^7.2.0",
		"@types/mocha": "^9.1.1",
		"@types/node": "16.x",
		"@typescript-eslint/eslint-plugin": "^5.27.0",
		"@typescript-eslint/parser": "^5.27.0",
		"eslint": "^8.16.0",
		"glob": "^8.0.3",
		"mocha": "^10.0.0",
		"typescript": "^4.7.2",
		"@vscode/test-electron": "^2.1.3"
	}
}